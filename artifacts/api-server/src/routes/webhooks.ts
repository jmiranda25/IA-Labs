// Clerk webhook endpoint removed — auth is now handled natively via JWT.
// This file is kept as a placeholder to avoid breaking imports if referenced elsewhere.
import { Router } from "express";

const router = Router();

export default router;
